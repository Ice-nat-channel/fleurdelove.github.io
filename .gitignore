# Ignore local admin Firebase config
# admin/firebase-config.js  <-- removed so admins can publish their client config if desired

# Ignore deploy trigger
deploy-trigger.txt

# Ignore service account keys
serviceAccountKey.json