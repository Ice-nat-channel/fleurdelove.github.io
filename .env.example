### Local and CI example of environment variables for Firebase config
# Copy to `.env` for local development (do NOT commit `.env`)
# In CI (GitHub Actions) add these values as repository secrets.

FIREBASE_API_KEY=YOUR_API_KEY
FIREBASE_AUTH_DOMAIN=YOUR_PROJECT.firebaseapp.com
FIREBASE_PROJECT_ID=YOUR_PROJECT_ID
FIREBASE_STORAGE_BUCKET=YOUR_PROJECT.appspot.com
FIREBASE_MESSAGING_SENDER_ID=YOUR_MESSAGING_SENDER_ID
FIREBASE_APP_ID=YOUR_APP_ID
FIREBASE_MEASUREMENT_ID=YOUR_MEASUREMENT_ID

SITE_URL=https://example.com/

# Usage:
# - Local: copy to `.env` and run `npm run build-config` to generate `admin/firebase-config.js`.
# - CI: the GitHub Actions workflow sets these from repository secrets and runs `npm run build-config`.
# Copy this file to .env and fill values. This file is safe to commit as example.
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=
FIREBASE_MEASUREMENT_ID=

# Optional: site URL you will publish to (used for documentation only)
SITE_URL=
